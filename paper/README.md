This directory contains the conference paper source for KeyperVPN.

Files:
- `keypervpn_ieee.tex`: IEEE conference paper source
- `benchmark.json`: measured local validation data from the implemented system

Compile on a machine with a TeX distribution that includes `IEEEtran`, `graphicx`, `booktabs`, `tikz`, and `url`.
